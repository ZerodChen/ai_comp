# Shared Utilities for AI Campus Projects

This directory contains common utilities that can be used across different weeks.

## Structure
- `common/` - Common functions and classes
- `utils/` - Utility functions
- `models/` - Shared data models
- `config/` - Configuration templates